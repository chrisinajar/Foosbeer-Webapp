var App = require('app');

var MatchLobby = App.View.extend({
	template: _.template([
	].join('')),

	initialize: function() {
		// do stuff?
		// what is even the model for this?.. fuck if i know..
	}
});

module.exports = MatchLobby;
